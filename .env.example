# Local dev: point at the standalone backend (server/ running `npm run dev`).
# On Vercel: leave VITE_API_URL empty (or unset) — the frontend and /api/* are
# served from the same domain, so requests should be same-origin relative paths.
VITE_API_URL=http://localhost:8787
